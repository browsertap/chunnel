#!/usr/bin/env node
optimist = require("optimist")
server   = require("..").server

argv = optimist.
usage('Usage: $0 [httpPort] [connectionPort]').
argv

server.listen(Number(argv._.shift()) || 80, Number(argv._.shift()))
